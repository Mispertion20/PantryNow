import React from 'react';
import { Text, View } from 'react-native';

const Recipes = () => {
  return (
    <View>
      <Text>Recipes</Text>
      {/* Add functionality for listing user-created recipes */}
    </View>
  );
};

export default Recipes;